@using Projekat.Models
@{
    Layout = null;

}

<h2>ProveriVoznje</h2>
    <table border="0">
        <tr>
            <th>
                Vreme kreiranja porudzbine
            </th>
            <th>
                Opcije
            </th>
        </tr>
        @foreach (KeyValuePair<string, Voznja> kv in ViewBag.voznje)
        {
            if (kv.Value.StatusVoznje == STATUS.NACEKANJU)
            {
                <tr>
                    <td>
                        @kv.Value.VremePorudzbine
                    </td>
                    <td>
                        <form action="/Taxi/DodeliVoznju" method="post">
                            <input type="hidden" value="@kv.Key" name="date" />
                            <input type="submit" value="Dodeli" />
                        </form>
                    </td>
                </tr>
            }
        }
    </table>
    <a href="/Taxi/GoToHomeAdmin">Vratite se na pocetnu</a>